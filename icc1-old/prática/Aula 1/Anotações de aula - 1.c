int x;      //"número inteiro" X - Cliva uma variavel chamada X que vai armazenar um espaço "inteiro"
x = 5;      //Nesse espaço de memória você está armazenando o valor 5

----

int x
int y

x = 5;
y = 10;

printf ("%d", x+y);     //colocar "%d" para números inteiros
printf ("%f", x+y);     //colocar "%f" para números racionais

"\n" tem que estar dentro das aspas
    Errado: ("o valor é %d", soma \n);
    Correto: ("o valor é %d \n", soma);

//Se usarmos uma variavel float, todas devem ser float (racionais)

   //imprime na tela o texto
    scanf ("%f", &v1);                  //recebe um valor que o usuário der - %f = número real - & = receber valor e adicionar como v1
